language: php
php:
  - 7.0
  
env:
  - DB=mysql
  
install:
  - composer install
  
script: phpunit --configuration phpunit.xml --coverage-text

after_success:
  - bash <(curl -s https://codecov.io/bash)
